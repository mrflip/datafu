
width = document.getElementById('map').offsetWidth;
height = width / 2;

# var svg, bg_g, places_g, tiles_g, path, graticule;
# var projection;
# var world_hires, world_lores;

# format = d3.format(".1f");
#   
# formatOrigin = (orientation) ->
#   "" +
#     format(Math.abs(orientation[1])) + "°" +
#     (orientation[1] > 0 ? "S" : "N") + ", " +
#     format(Math.abs(orientation[0])) + "°" +
#     (orientation[0] > 0 ? "W" : "E")


console.log d3
console.log d3.select("#lat_ctl").attr("value", "hello");
